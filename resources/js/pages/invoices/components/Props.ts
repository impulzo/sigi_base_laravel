export interface Product{
    id: number,
    name: string,
    description: string,
    price: number,
    product_code: string,
    unit_code: string,
    quantity: number,
    subtotal: number,
    iva: number,
    total: number
}